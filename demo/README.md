Before running the demo install kockout and browserify
```
npm install knockout
npm install -g browserify
```

Create the bundle
```
browserify main.js -o bundle.js
```

All set! Now visit index.html and try it out!